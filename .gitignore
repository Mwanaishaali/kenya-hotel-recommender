# Secrets
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# data/photos/ is intentionally NOT ignored anymore — the deployed
# backend serves these images, so they need to be in the repo.
